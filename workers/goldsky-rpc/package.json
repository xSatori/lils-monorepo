{
  "name": "@lils/goldsky-rpc-worker",
  "private": true,
  "description": "Cloudflare Worker: JSON-RPC proxy to Goldsky Edge (secret server-side only).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250404.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.114.0"
  }
}
